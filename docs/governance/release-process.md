# Release Process

There is no automated release process defined in this repository.

Suggested workflow:

1. Merge changes to `main`.
2. Tag a version (for example `v1.2.3`).
3. Build and deploy via your CI/CD or Cloud Run pipeline.
